# Detection des maxima

Dossier reserve pour tester une approche de detection des maximums independante du GMM/KDE actuel.

Point de depart conseille :

- lire les FME via `src.bat_preprocessing.preprocess_bat_file`;
- isoler l'algorithme dans un module ou script de ce dossier;
- enregistrer les figures dans `plots/maxima_detection/`;
- ajouter un petit jeu synthetique ou un test dedie quand l'approche devient stable.
